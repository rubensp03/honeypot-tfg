# ============================================================
# TFG - Honeypot para el estudio estadístico de amenazas informáticas
# mediante honeypots
#
# Instalación: pip install -r requirements.txt
# ============================================================

# --- IA / LLM Classifier (ai-classifier) ---
aiohttp>=3.9.0
pandas>=2.0.0
requests>=2.31.0
pyyaml>=6.0.1

# --- Generación de Gráficas (graficas + analisis_tcp) ---
matplotlib>=3.8.0
seaborn>=0.13.0
geopandas>=0.14.0
numpy>=1.26.0

# --- Diagramas de Arquitectura (graficas/src/diagrama_*) ---
diagrams>=0.23.0

# --- SSH / Cowrie ---
# Solo usa stdlib (re), sin dependencias adicionales
